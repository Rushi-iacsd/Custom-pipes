import { FilterPalyersPipe } from './filter-palyers.pipe';

describe('FilterPalyersPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterPalyersPipe();
    expect(pipe).toBeTruthy();
  });
});
